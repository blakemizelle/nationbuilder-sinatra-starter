<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NationBuilder Sinatra Starter</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            line-height: 1.6;
            color: #333;
        }
        .header {
            text-align: center;
            margin-bottom: 40px;
            padding-bottom: 20px;
            border-bottom: 2px solid #e1e5e9;
        }
        .header h1 {
            color: #2c3e50;
            margin-bottom: 10px;
        }
        .header p {
            color: #7f8c8d;
            margin: 0;
        }
        .button {
            display: inline-block;
            background: #3498db;
            color: white;
            padding: 12px 24px;
            text-decoration: none;
            border-radius: 6px;
            font-weight: 500;
            transition: background-color 0.2s;
        }
        .button:hover {
            background: #2980b9;
        }
        .button.secondary {
            background: #95a5a6;
        }
        .button.secondary:hover {
            background: #7f8c8d;
        }
        .status-card {
            background: #f8f9fa;
            border: 1px solid #e9ecef;
            border-radius: 8px;
            padding: 20px;
            margin: 20px 0;
        }
        .status-card h3 {
            margin-top: 0;
            color: #2c3e50;
        }
        pre {
            background: #2c3e50;
            color: #ecf0f1;
            padding: 15px;
            border-radius: 6px;
            overflow-x: auto;
            font-size: 14px;
        }
        .nav {
            text-align: center;
            margin: 20px 0;
        }
        .nav a {
            margin: 0 10px;
        }
        .error {
            background: #e74c3c;
            color: white;
            padding: 15px;
            border-radius: 6px;
            margin: 20px 0;
        }
        .success {
            background: #27ae60;
            color: white;
            padding: 15px;
            border-radius: 6px;
            margin: 20px 0;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>NationBuilder Sinatra Starter</h1>
        <p>OAuth 2.0 with PKCE Demo Application</p>
    </div>

    <div class="nav">
        <a href="/" class="button">Home</a>
        <% if token_store.get_tokens(session.id) %>
            <a href="/status" class="button">Status</a>
            <a href="/logout" class="button secondary">Logout</a>
        <% else %>
            <a href="/login" class="button">Connect to NationBuilder</a>
        <% end %>
    </div>

    <% if token_store.get_tokens(session.id) %>
        <div class="status-card">
            <h3>🔗 Connection Status</h3>
            <% tokens = token_store.get_tokens(session.id) %>
            <p><strong>Status:</strong> 
                <% if token_store.token_expired?(tokens) %>
                    <span style="color: #e74c3c;">Expired</span>
                <% elsif token_store.token_expires_soon?(tokens) %>
                    <span style="color: #f39c12;">Expires Soon</span>
                <% else %>
                    <span style="color: #27ae60;">Connected</span>
                <% end %>
            </p>
            <p><strong>Connected:</strong> <%= Time.at(tokens['expires_at'] - tokens['expires_in'].to_i).strftime('%Y-%m-%d %H:%M:%S UTC') %></p>
            <p><strong>Expires:</strong> <%= Time.at(tokens['expires_at']).strftime('%Y-%m-%d %H:%M:%S UTC') %></p>
            <p><strong>Nation:</strong> <%= ENV['NB_BASE_URL'].gsub('https://', '').gsub('.nationbuilder.com', '') %></p>
            <p><strong>Scope:</strong> <%= tokens['scope'] %></p>
            <% if defined?(user_name) && user_name %>
                <p><strong>User:</strong> <%= user_name %></p>
            <% end %>
        </div>
    <% end %>

    <%= yield %>

    <div class="nav">
        <a href="/health" class="button secondary">Health Check</a>
    </div>
</body>
</html>